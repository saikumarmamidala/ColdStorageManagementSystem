@model IEnumerable<MyFirstProj.Models.Invoice>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<style>
    table {
        font-family: Arial, Helvetica, sans-serif;
        border-collapse: collapse;
        width: 100%;
    }

         td,  th {
            border: 1px solid #ddd;
            padding: 6px;
        }

         tr:nth-child(even) {
            background-color: #f2f2f2;
        }

         tr:hover {
            background-color: #ddd;
        }

    th {
        padding-top: 20px;
        padding-bottom: 20px;
        text-align: left;

        background-color: #034f84;
         color: white;
    }
</style>
<body style="background-color:#deeaee">
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Customer.CustomerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OutgoingLogistic.OutgoingLogisticName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Payment.PaymentType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OutgoingLogisticQuantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Time)
            </th>
            <th>Edit Options</th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Customer.CustomerName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.OutgoingLogistic.OutgoingLogisticName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Payment.PaymentType)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.OutgoingLogisticQuantity)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Date)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Time)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.BillNumber }) |
                    @Html.ActionLink("Details", "Details", new { id = item.BillNumber }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.BillNumber })
                </td>
            </tr>
        }

    </table>
</body>